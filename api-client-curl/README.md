# Sample Project: `api-client-curl`

```shell
goalSetId="..."

curl \
  -H "X-ID-Token: $X_ID_TOKEN" \
  -H "Accept: application/json" \
  "https://demo.portal.aixigo.cloud/goals-and-constraints/goals/${goalSetId}"
```
